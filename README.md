# SikorskyArchive
A system-level encryption tool built in C++ that packs directory structures into opaque, AES-256-CBC secured archives. Features manual OpenSSL integration and a custom binary format.
